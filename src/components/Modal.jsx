// Global modal shell — ProjectsPage and BlogPage render their own inline modals.
// This component is kept as a placeholder; you can extend it for global popups.
export default function Modal() {
  return null
}
